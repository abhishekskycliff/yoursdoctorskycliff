import 'package:flutter/material.dart';

class SecurityPin extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("hello pin screen"),
      ),
    );
  }

}